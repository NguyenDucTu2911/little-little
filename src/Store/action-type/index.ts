export enum ActionType {
    CHECKTICKER = "checkticker"
}

export interface Customer {
    Name: string,
    Phone: number,
    id: string,
    email: string,
    quantity: number,
    Date: Date,
    package: string

}